# ---------------------------------------------------------------------------
# 运行产物：上传的图片、标注图、评测报告、日志、网页截图缓存（约 46 MB）。
# 全部可由代码重新生成（objloc/samples.py 的 build() 会自动重抓网页截图），
# 且可能包含用户上传的隐私图片，因此不入库。
# 注：AGENTS.md 第 7 节引用的 runs/benchmark_*/report.json 属这类产物，
#     照着那里的“复现”命令重跑即可得到同样的数字。
# ---------------------------------------------------------------------------
runs/

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# ---- 密钥 / 本地环境 ----
# 真实 DEEPSEEK_API_KEY 只存在于 Windows 用户级环境变量里，仓库内只保留 .env.example
.env
.env.local
*.local

# ---- 编辑器 / 操作系统 ----
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ---- 临时文件 ----
*.tmp
*.bak
*.orig
