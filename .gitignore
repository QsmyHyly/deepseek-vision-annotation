# ---------------------------------------------------------------------------
# 运行产物：上传的图片、标注图、评测报告、日志、网页截图缓存（约 46 MB）。
# 全部可由代码重新生成（objloc/samples.py 的 build() 会自动重抓网页截图），
# 且可能包含用户上传的隐私图片，因此不入库。
# 注：AGENTS.md 第 7 节引用的 runs/benchmark_*/report.json 属这类产物，
#     照着那里的“复现”命令重跑即可得到同样的数字。
# ---------------------------------------------------------------------------
runs/

# --- 兜底安全带：annotate() 不传 output_dir 时的落点就是"当前工作目录" ---
# 正常情况下它该落在 runs/scratch/（见 AGENTS.md §4.9），上面那行已经盖住了。
# ⚠️ 这一行**不是许可证**，是防止某次漏传 output_dir 的散图被 git add -A 顺手收进提交
#    （2026-09-18 真发生过一次：两个 20 KB 的 PNG 进了 commit，只能再开一个清理提交）。
#    看到根目录冒出这种文件，请去修调用方，别靠这条忽略当无事发生。
annotated_*.png

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# ---- 密钥 / 本地环境 ----
# 真实 DEEPSEEK_API_KEY 只存在于 Windows 用户级环境变量里，仓库内只保留 .env.example
.env
.env.local
*.local

# 用户偏好（网页「识别参数」保存的思考模式 / 工具开关 / 工具轮数 / 提示词，见 AGENTS.md §4.10）。
# ⚠️ 必须单独写一行：上面的 *.local 只匹配以 .local 结尾的文件名，
#    匹配不到 config.local.json 这种「中间夹 local」的名字。
config.local.json

# ---- 编辑器 / 操作系统 ----
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ---- 临时文件 ----
*.tmp
*.bak
*.orig

# 快捷方式（install-shortcut.ps1 的产物）。不提交是有意的：.lnk 里写死的是
# 这台机器上的绝对路径，换台机器/换个目录就是坏链接 —— 提交生成器，不提交产物。
*.lnk
